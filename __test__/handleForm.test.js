import { handleForm } from "../src/client/js/funcs";

describe("Testing the submit functionality", () => {
  test("Testing the handleForm() function", () => {
    expect(handleForm).toBeDefined();
  });
});
